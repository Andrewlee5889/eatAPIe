publicapis:
  root_url: 'https://api.publicapis.org/'
  endpoints:
    GET:
      blah: entries
      bleh: entries
open_ai:
  root_url: 'https://api.openai.com/v1/'
  auth_header: 'Bearer {OPEN_AI_API_KEY}'
  content_type: 'application/json'
  endpoints:
    GET:
      models: models
      model: models/{model_id}/
cat:
  root_url: 'https://catfact.ninja/'
  endpoints:
    GET:
      fact: fact
ipify:
  root_url: 'https://api.ipify.org/'
  endpoints:
    GET:
      json: ''
